export interface Trend {
  id: string;
  category: string;
  title: string;
  postsCount: number;
}

export interface UserSuggestion {
  id: string;
  name: string;
  username: string;
  avatarUrl: string;
}
